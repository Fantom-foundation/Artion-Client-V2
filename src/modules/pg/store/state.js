/**
 * PG state object
 * @typedef {Object} PgState
 * @property {boolean} showModal State of modal visibility
 */

/** @type {PgState} */
export const state = {
    showModal: true,
};

export const storeInLocalStorage = [];
